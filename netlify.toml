# Netlify — ElephantLabs (Astro hybrid SSR)
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Naikkan batas body untuk request TTS (teks hingga 2000 karakter + JSON)
[functions]
  included_files = ["dist/**"]
